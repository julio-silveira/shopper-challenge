import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#2da77a'
    },
    divider: '#2da77a',
    background: {
      default: '#FAFEFC',
      paper: '#E2F4EF'
    }
  }
})

export default theme
